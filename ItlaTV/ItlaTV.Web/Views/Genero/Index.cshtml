@model IEnumerable<ItlaTV.Web.Models.Genero.GeneroViewModel>

@{
    ViewData["Title"] = "Generos";
}

<h1>Generos</h1>

<p>
    <a asp-action="Create">Nuevo genero</a>
</p>
@if (Model.Count() == 0)
{
    <tr>No existen generos</tr>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nombre)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nombre)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id=item.GeneroId  }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.GeneroId  })
                    </td>
                </tr>
            }
        </tbody>
    </table>
}